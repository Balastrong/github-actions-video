name: Label PR

on:
  pull_request:
    types: [opened, reopened]

jobs:
  label:
    runs-on: ubuntu-latest      
    steps:
      - uses: actions/checkout@main
      - uses: Balastrong/github-typescript-action-test@main
        with:
          label: needs-review
          gh-token: ${{ secrets.GITHUB_TOKEN }}
